# Chatbot service GĐ2 — client của shop_api (chatbot-architecture.md §0/§8: service riêng).
#
# LÕI chạy bằng stdlib thuần (state machine, PII, session in-memory, NLU/NLG fake, HTTP
# qua urllib) → unit-test bước ③④⑤ KHÔNG cần cài gì (mẹo test §9). Các adapter thật bên
# dưới bật lên khi cấu hình env — cài thêm khi lên staging/prod.

Flask>=3.0            # web layer /chat/message (đồng bộ stack với shop_api)

# --- Adapter production (tùy chọn; lõi không phụ thuộc) ---
# redis>=5.0          # RedisSessionStore (Q5) — mặc định dev dùng in-memory
# httpx>=0.27         # thay urllib nếu muốn connection pool/async

# --- RAG lai: vector + rerank (tùy chọn, ~2-3GB gồm cả torch) ---
# Chỉ cần khi RAG_BACKEND=hybrid. Thiếu -> app/retrieval.py tự lùi về BM25 thuần, service
# vẫn chạy và test vẫn xanh (mọi import nặng nằm TRONG hàm, có try/except).
#
#   python -m pip install chromadb sentence-transformers pyvi
#
# chromadb>=0.5              # vector store nhúng, persist ra data/faq_chroma/
# sentence-transformers>=3.0 # bi-encoder + CrossEncoder (kéo theo torch, transformers)
# pyvi>=0.1.1                # tách từ tiếng Việt — PhoBERT BẮT BUỘC input đã tách từ.
#                            # Công cụ chuẩn py_vncorenlp là jar Java (cần JDK) nên dùng pyvi.
